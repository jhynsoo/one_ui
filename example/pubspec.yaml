name: one_ui_example
description: A Flutter application showcasing how to use one_ui package.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  dynamic_color: ^1.8.1
  flutter:
    sdk: flutter

  one_ui:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
