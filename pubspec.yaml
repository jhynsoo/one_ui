name: one_ui
description: Flutter widgets implementing Samsung One UI. Based on the official documentation.
version: 0.1.0
homepage: https://github.com/tockiib/one_ui

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
